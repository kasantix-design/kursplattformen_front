[build]
  # 🏗️ Bygg Vite-prosjektet
  command = "npm run build"
  publish = "dist"

[[redirects]]
  # 🔁 Fanger opp alle ruter for SPA-routing (React-router)
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  # 🛠️ Fiks for MIME-type-problem: sørg for at .js får riktig content-type
  for = "/assets/*"
  [headers.values]
    Content-Type = "application/javascript"
